import { SanctionToEmploye } from './sanction-to-employe';

describe('SanctionToEmploye', () => {
  it('should create an instance', () => {
    expect(new SanctionToEmploye()).toBeTruthy();
  });
});
